<p id="notice"><%= notice %></p>

<h1>Railways List</h1>

<table>
  <thead>
  <tr>
  <tr>Number</tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <% @trains.each do |trains| %>
      <tr>
        <td><%= train.number %> </td>
        <td><%= link_to 'Show', train %></td>
        <td><%= link_to 'Edit', edit_train_path(railway) %></td>
        <td><%= link_to 'Destroy', train, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Railway', new_railway_path %>
